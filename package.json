{
	"scripts": {
		"dev": "run-p dev:*",
		"dev:server-tsc": "tsc -p ./server --watch",
		"dev:server-node": "nodemon server/prod/server.js",
		"dev:client": "npm start --prefix client",
		"build": "run-p build:*",
		"build:server": "tsc -p ./server",
		"build:client": "npm run dev --prefix client"
	},
	"dependencies": {
		"express": "^4.17.1",
		"mysql2": "^2.2.5"
	},
	"devDependencies": {
		"@types/express": "^4.17.8",
		"@types/node": "^14.14.5",
		"nodemon": "^2.0.7",
		"npm-run-all": "^4.1.5",
		"typescript": "^4.0.5"
	}
}
